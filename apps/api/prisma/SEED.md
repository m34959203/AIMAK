# Database Seeding

## Автоматический seed при деплое

Seed скрипт автоматически запускается при деплое на Render.com после миграций.

## Создаваемые данные

### Admin пользователь
- **Email:** `admin@aimak.kz`
- **Пароль по умолчанию:** `admin123456` (если не установлена переменная `ADMIN_PASSWORD`)
- **Роль:** ADMIN
- **Статус:** Активен и верифицирован

### Базовые категории
1. Жаңалықтар / Новости (news)
2. Саясат / Политика (politics)
3. Экономика / Экономика (economy)
4. Мәдениет / Культура (culture)
5. Спорт / Спорт (sport)

## Настройка пароля админа

### На Render.com
В настройках сервиса `aimak-api` добавьте переменную окружения:
```
ADMIN_PASSWORD=ваш_сильный_пароль
```

### Локально
```bash
# В файле .env
ADMIN_PASSWORD=your_secure_password

# Или при запуске
ADMIN_PASSWORD=your_password pnpm db:seed
```

## Запуск вручную

### Локально
```bash
cd apps/api
pnpm db:seed
```

### На Render.com
Используйте Shell в дашборде Render:
```bash
cd apps/api
pnpm db:seed
```

## Важно! 🔒

1. **Смените пароль админа** сразу после первого входа
2. Установите сильный пароль через переменную `ADMIN_PASSWORD` на Render
3. Seed скрипт НЕ перезапишет существующего админа
4. Если админ уже существует, скрипт пропустит создание

## Проверка

После seed:
1. Откройте фронтенд приложения
2. Войдите с данными:
   - Email: `admin@aimak.kz`
   - Пароль: значение из `ADMIN_PASSWORD` или `admin123456`
3. Проверьте, что вы имеете права админа

## Troubleshooting

### Ошибка "Admin user already exists"
Это нормально - админ уже создан. Если забыли пароль, можно:
1. Удалить админа из базы через Prisma Studio
2. Запустить seed заново

### Seed не запускается на Render
Проверьте логи билда - команда `pnpm db:seed` должна быть в buildCommand.
