# 🔧 Исправление проблемы с отображением статей

## Проблема
На сайте не отображаются статьи из-за двух проблем:
1. **Неправильный API URL** в переменных окружения Frontend
2. **Нет статей** в базе данных

## ✅ Решение

### Шаг 1: Исправить переменные окружения в Render

#### Для сервиса `aimak-web` (Frontend):

1. Откройте https://dashboard.render.com/
2. Выберите сервис **aimak-web**
3. Перейдите в **Environment**
4. Найдите переменную **NEXT_PUBLIC_API_URL**
5. Измените значение на:
   ```
   https://aimak-api-w8ps.onrender.com/api
   ```
6. Найдите переменную **API_URL**
7. Измените значение на:
   ```
   https://aimak-api-w8ps.onrender.com/api
   ```
8. Нажмите **Save Changes**
9. Сервис автоматически перезапустится

#### Для сервиса `aimak-api` (Backend):

1. Выберите сервис **aimak-api**
2. Перейдите в **Environment**
3. Проверьте переменную **CORS_ORIGIN**
4. Она должна содержать только hostname (без https://):
   ```
   aimak-web-rvep.onrender.com
   ```
5. Проверьте **FRONTEND_URL** - то же самое:
   ```
   aimak-web-rvep.onrender.com
   ```

### Шаг 2: Заполнить базу данных статьями

У вас есть несколько опций:

#### Опция A: Импорт из WordPress (если есть)
```bash
cd apps/api
pnpm import:wordpress:full
```

#### Опция B: Создать тестовые статьи через админ-панель
1. Откройте https://aimak-web-rvep.onrender.com/admin/login
2. Войдите с учетными данными:
   - Email: `admin@aimakakshamy.kz`
   - Password: `admin123`
3. Перейдите в **Статьи** → **Создать статью**
4. Создайте несколько статей

#### Опция C: Запустить расширенный seed (рекомендуется)
```bash
cd apps/api
pnpm db:seed:articles
```

Этот скрипт создаст тестовые статьи для каждой категории.

### Шаг 3: Проверка

После выполнения шагов 1 и 2:

1. Проверьте API напрямую:
   ```bash
   curl https://aimak-api-w8ps.onrender.com/api/articles
   ```
   Должен вернуть массив статей (не пустой)

2. Откройте сайт https://aimak-web-rvep.onrender.com/
3. Статьи должны отображаться

## 🚨 Важно!

После изменения переменных окружения в Render:
- Frontend и Backend автоматически перезапустятся
- Это займет 2-3 минуты
- Дождитесь зеленого статуса "Live" в дашборде

## 📝 Проверка текущего состояния

### Проверить API:
```bash
# Список статей
curl https://aimak-api-w8ps.onrender.com/api/articles

# Категории
curl https://aimak-api-w8ps.onrender.com/api/categories

# Health check
curl https://aimak-api-w8ps.onrender.com/api/health
```

### Проверить конфигурацию Frontend:
```bash
# Откройте DevTools в браузере (F12)
# В Console выполните:
console.log(process.env.NEXT_PUBLIC_API_URL)
```

Должно вывести: `https://aimak-api-w8ps.onrender.com/api`

## 🔄 Если проблема не решилась

1. Проверьте логи в Render Dashboard:
   - **aimak-api** → Logs
   - **aimak-web** → Logs

2. Убедитесь, что DATABASE_URL правильный в переменных API

3. Попробуйте Manual Deploy для обоих сервисов
