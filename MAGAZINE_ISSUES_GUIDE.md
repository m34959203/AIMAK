# 📰 Руководство по работе с PDF-выпусками журнала

## ✅ Что было реализовано

### Backend (API)
- ✅ Prisma модель `MagazineIssue` с полями:
  - Метаданные: номер, год, месяц, дата публикации
  - Двуязычные поля: название и описание (каз/рус)
  - PDF файл: имя, URL, размер, количество страниц
  - Обложка: URL изображения
  - Метрики: счетчики просмотров и скачиваний
  - Статус: опубликован/черновик, закреплен
- ✅ API endpoints `/magazine-issues` (CRUD операции)
- ✅ Загрузка PDF до 50MB (Multer конфигурация)
- ✅ Автоматическое создание директории `uploads/magazines/`
- ✅ Доступ только для Editor/Admin ролей

### Frontend
- ✅ Админ-страница управления: `/admin/magazine-issues`
- ✅ Публичная страница просмотра: `/{lang}/issues`
- ✅ PDF-вьювер с навигацией и масштабированием
- ✅ Обновленная навигация ("Архив" → "Выпуски")
- ✅ Интеграция с дашбордом админки

---

## 🚀 Начало работы

### 1. Применить миграцию базы данных

**ВАЖНО:** Перед использованием нужно применить миграцию Prisma:

```bash
cd apps/api
npx prisma migrate dev --name add_magazine_issues
# или
npx prisma migrate deploy
```

Это создаст таблицу `magazine_issues` в базе данных.

### 2. Убедиться, что директория для загрузок создана

Модуль автоматически создает директорию, но можно проверить:

```bash
ls -la apps/api/uploads/magazines/
```

### 3. Запустить приложение

```bash
# Из корня проекта
pnpm dev
```

- Frontend: http://localhost:3000
- Backend API: http://localhost:4000

---

## 📖 Как использовать

### Для Редакторов/Админов

#### 1. Загрузка нового выпуска

1. Войдите в систему как Editor или Admin
2. Перейдите в админ-панель: http://localhost:3000/admin
3. Нажмите на карточку "Выпуски журнала"
4. Нажмите "+ Добавить выпуск"
5. Заполните форму:
   - **Номер выпуска** (обязательно): например, 1
   - **Год** (обязательно): 2025
   - **Месяц** (обязательно): 1-12
   - **Дата публикации** (обязательно): выберите дату
   - **Название (Каз)** (обязательно): например, "Қаңтар айының шығарылымы"
   - **Название (Рус)** (обязательно): например, "Январский выпуск"
   - **Описание** (опционально): краткое описание на обоих языках
   - **PDF файл** (обязательно): выберите файл (до 50MB)
   - **Обложка** (опционально): загрузите изображение обложки
   - **Количество страниц** (опционально): например, 120
   - Отметьте чекбоксы:
     - ✅ Опубликовать (если хотите сразу опубликовать)
     - ✅ Закрепить (для важных выпусков)
6. Нажмите "Создать выпуск"

#### 2. Управление выпусками

В таблице выпусков вы можете:
- **Открыть** - просмотреть PDF в новой вкладке
- **Удалить** - удалить выпуск (с подтверждением)
- Видеть статистику: просмотры, скачивания
- Видеть статус: опубликован/черновик, закреплен

### Для пользователей сайта

#### Просмотр выпусков

1. Откройте сайт
2. В меню навигации нажмите "Выпуски" (или "Шығарылымдар" на каз.)
3. Вы увидите:
   - **Левая панель**: список всех выпусков, сгруппированных по годам
   - **Фильтр по годам**: выберите конкретный год
   - **Правая панель**: область для просмотра PDF

#### Чтение выпуска

1. Нажмите на любой выпуск в списке слева
2. PDF отобразится справа с панелью управления:
   - **◄ ►** - навигация по страницам
   - **- +** - изменение масштаба (50%-200%)
   - **Скачать PDF** - скачивание файла (увеличивает счетчик)
3. Прокручивайте для просмотра

---

## 🔧 Технические детали

### API Endpoints

| Метод | Путь | Описание | Доступ |
|-------|------|----------|--------|
| GET | `/magazine-issues` | Получить все выпуски | Публичный |
| GET | `/magazine-issues/year/:year` | Выпуски по году | Публичный |
| GET | `/magazine-issues/:id` | Один выпуск | Публичный |
| POST | `/magazine-issues` | Создать выпуск | Editor/Admin |
| PATCH | `/magazine-issues/:id` | Обновить выпуск | Editor/Admin |
| DELETE | `/magazine-issues/:id` | Удалить выпуск | Editor/Admin |
| POST | `/magazine-issues/:id/view` | +1 просмотр | Публичный |
| POST | `/magazine-issues/:id/download` | +1 скачивание | Публичный |

### Структура файлов

```
apps/
├── api/
│   ├── prisma/
│   │   └── schema.prisma              # Модель MagazineIssue
│   ├── src/
│   │   └── magazine-issues/
│   │       ├── dto/
│   │       │   ├── create-magazine-issue.dto.ts
│   │       │   └── update-magazine-issue.dto.ts
│   │       ├── magazine-issues.controller.ts
│   │       ├── magazine-issues.service.ts
│   │       └── magazine-issues.module.ts
│   └── uploads/
│       └── magazines/                 # PDF файлы здесь
└── web/
    └── src/
        ├── app/
        │   ├── [lang]/
        │   │   └── issues/
        │   │       └── page.tsx       # Публичная страница
        │   └── admin/
        │       └── magazine-issues/
        │           └── page.tsx       # Админ-страница
        ├── components/
        │   └── pdf-viewer.tsx         # PDF viewer компонент
        ├── hooks/
        │   └── use-magazine-issues.ts # React hooks
        └── types/
            └── index.ts               # TypeScript типы
```

### Используемые библиотеки

- **Backend:**
  - `@nestjs/platform-express` - файловая загрузка
  - `multer` - обработка multipart/form-data
  - `@prisma/client` - ORM

- **Frontend:**
  - `react-pdf` - отображение PDF
  - `pdfjs-dist` - PDF.js worker
  - `@tanstack/react-query` - управление состоянием API

---

## 🐛 Известные проблемы и решения

### Проблема: PDF не отображается

**Решение:** Убедитесь, что:
1. PDF файл был успешно загружен (проверьте `uploads/magazines/`)
2. URL файла корректен (проверьте в БД)
3. Статический файл доступен по URL

### Проблема: Ошибка при загрузке "Only PDF files are allowed"

**Решение:** Файл должен иметь расширение `.pdf` и MIME type `application/pdf`

### Проблема: Превышен лимит размера

**Решение:** Текущий лимит 50MB. Для увеличения измените в `magazine-issues.module.ts`:
```typescript
limits: {
  fileSize: 100 * 1024 * 1024, // 100MB
}
```

---

## 📊 Пример использования

### Пример загрузки через API (curl)

```bash
curl -X POST http://localhost:4000/magazine-issues \
  -H "Authorization: Bearer YOUR_TOKEN" \
  -F "file=@/path/to/magazine.pdf" \
  -F "issueNumber=1" \
  -F "year=2025" \
  -F "month=1" \
  -F "publishDate=2025-01-15T00:00:00Z" \
  -F "titleKz=Қаңтар айының шығарылымы" \
  -F "titleRu=Январский выпуск" \
  -F "descriptionKz=Қысқа сипаттама" \
  -F "descriptionRu=Краткое описание" \
  -F "pagesCount=120" \
  -F "isPublished=true" \
  -F "isPinned=false"
```

### Пример ответа

```json
{
  "id": "uuid-here",
  "issueNumber": 1,
  "year": 2025,
  "month": 1,
  "publishDate": "2025-01-15T00:00:00.000Z",
  "titleKz": "Қаңтар айының шығарылымы",
  "titleRu": "Январский выпуск",
  "descriptionKz": "Қысқа сипаттама",
  "descriptionRu": "Краткое описание",
  "pdfFilename": "magazine-1737037200000-123456789.pdf",
  "pdfUrl": "https://your-domain.com/uploads/magazines/magazine-1737037200000-123456789.pdf",
  "fileSize": 5242880,
  "pagesCount": 120,
  "coverImageUrl": null,
  "viewsCount": 0,
  "downloadsCount": 0,
  "isPublished": true,
  "isPinned": false,
  "uploadedById": "user-uuid",
  "createdAt": "2025-01-16T10:00:00.000Z",
  "updatedAt": "2025-01-16T10:00:00.000Z"
}
```

---

## 🎯 Следующие шаги (опционально)

Если нужно расширить функциональность:

1. **OCR для извлечения текста** - добавить `pdf-parse` для индексации
2. **Генерация превью** - создавать изображения страниц
3. **Водяные знаки** - защита PDF
4. **Архивация** - старые выпуски в ZIP
5. **RSS feed** - подписка на новые выпуски
6. **Email уведомления** - оповещение о новых выпусках

---

## 📝 Changelog

### v1.0.0 (2025-01-16)
- ✅ Реализована базовая функциональность
- ✅ Админ-интерфейс для управления
- ✅ Публичная страница с PDF-вьювером
- ✅ Метрики просмотров и скачиваний
- ✅ Двуязычная поддержка
- ✅ Исправлена SSR ошибка react-pdf

---

## 🤝 Поддержка

Если возникли вопросы:
1. Проверьте логи backend: `cd apps/api && pnpm dev`
2. Проверьте логи frontend: `cd apps/web && pnpm dev`
3. Проверьте консоль браузера (F12)

**Успешной работы с выпусками журнала! 🎉**
