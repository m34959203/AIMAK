# 🚀 Руководство по деплою на Render.com

Это подробное руководство по развертыванию проекта Aimak Akshamy на платформе Render.com.

## 📋 Содержание
1. [Подготовка](#подготовка)
2. [Деплой через Render Blueprint](#деплой-через-render-blueprint)
3. [Ручной деплой](#ручной-деплой)
4. [Настройка переменных окружения](#настройка-переменных-окружения)
5. [После деплоя](#после-деплоя)
6. [Troubleshooting](#troubleshooting)

---

## 🔧 Подготовка

### 1. Создать аккаунт на Render.com
1. Перейдите на https://render.com
2. Зарегистрируйтесь (можно через GitHub)
3. Подтвердите email

### 2. Подключить GitHub репозиторий
1. В Render Dashboard → Settings → Connected Accounts
2. Подключите ваш GitHub аккаунт
3. Дайте доступ к репозиторию AIMAK

---

## 🎯 Деплой через Render Blueprint (Рекомендуется)

Этот метод автоматически создаст все необходимые сервисы из конфигурации `render.yaml`.

### Шаг 1: Создать Blueprint
1. В Render Dashboard нажмите **"New +"** → **"Blueprint"**
2. Выберите ваш GitHub репозиторий `AIMAK`
3. Render автоматически найдет файл `render.yaml`
4. Нажмите **"Apply"**

### Шаг 2: Настроить переменные окружения
Render создаст следующие сервисы:
- `aimak-postgres` - PostgreSQL база данных (Free tier)
- `aimak-redis` - Redis instance (Free tier)
- `aimak-api` - Backend API (Free tier)
- `aimak-web` - Frontend (Free tier)

**Важно:** Убедитесь, что переменная `JWT_SECRET` была сгенерирована автоматически или задайте её вручную!

### Шаг 3: Дождаться деплоя
- Render автоматически задеплоит все сервисы
- Первый деплой может занять 10-15 минут
- Отслеживайте прогресс в разделе "Events"

---

## 🛠️ Ручной деплой

Если вы хотите больше контроля, можно создать сервисы вручную.

### 1. Создать PostgreSQL базу данных

1. **New +** → **PostgreSQL**
2. Параметры:
   - Name: `aimak-postgres`
   - Database: `aimak_db`
   - User: `aimak_user`
   - Region: выберите ближайший к вам
   - Plan: **Free**
3. Создайте базу данных
4. **Скопируйте Internal Database URL** (понадобится для API)

### 2. Создать Redis instance

1. **New +** → **Redis**
2. Параметры:
   - Name: `aimak-redis`
   - Region: тот же, что и у PostgreSQL
   - Plan: **Free**
3. Создайте Redis
4. **Скопируйте Internal Redis URL**

### 3. Задеплоить Backend API

1. **New +** → **Web Service**
2. Параметры:
   - Repository: выберите `AIMAK`
   - Name: `aimak-api`
   - Runtime: **Node**
   - Region: тот же, что и БД
   - Branch: `main` (или ваша ветка)
   - Root Directory: оставьте пустым
   - Build Command:
     ```bash
     cd apps/api && pnpm install && pnpm prisma generate && pnpm build
     ```
   - Start Command:
     ```bash
     cd apps/api && pnpm prisma migrate deploy && pnpm start:prod
     ```
   - Plan: **Free**

3. **Environment Variables** (добавьте следующие):
   ```
   NODE_ENV=production
   PORT=4000
   DATABASE_URL=<Internal Database URL из шага 1>
   REDIS_HOST=<Redis Host из шага 2>
   REDIS_PORT=<Redis Port из шага 2>
   JWT_SECRET=<сгенерируйте случайную строку минимум 32 символа>
   JWT_EXPIRES_IN=7d
   FRONTEND_URL=https://<будет известен после деплоя frontend>
   ```

4. **Advanced** → Health Check Path: `/api/health`
5. Нажмите **Create Web Service**

### 4. Задеплоить Frontend

1. **New +** → **Web Service**
2. Параметры:
   - Repository: `AIMAK`
   - Name: `aimak-web`
   - Runtime: **Node**
   - Region: тот же
   - Branch: `main`
   - Root Directory: оставьте пустым
   - Build Command:
     ```bash
     cd apps/web && pnpm install && pnpm build
     ```
   - Start Command:
     ```bash
     cd apps/web && pnpm start
     ```
   - Plan: **Free**

3. **Environment Variables**:
   ```
   NODE_ENV=production
   NEXT_PUBLIC_API_URL=https://<URL вашего aimak-api сервиса>
   NEXT_PUBLIC_APP_NAME=Aimak Akshamy
   NEXT_PUBLIC_APP_DESCRIPTION=City Newspaper
   ```

4. Нажмите **Create Web Service**

### 5. Обновить FRONTEND_URL в API

1. Вернитесь к сервису `aimak-api`
2. Environment → Edit
3. Обновите `FRONTEND_URL` на URL вашего `aimak-web` сервиса
4. Сохраните (это вызовет редеплой)

---

## 🔐 Настройка переменных окружения

### Backend API (aimak-api)

| Переменная | Значение | Обязательная |
|------------|----------|--------------|
| `NODE_ENV` | `production` | ✅ |
| `PORT` | `4000` | ✅ |
| `DATABASE_URL` | Internal Database URL | ✅ |
| `REDIS_HOST` | Redis Internal Host | ✅ |
| `REDIS_PORT` | Redis Internal Port | ✅ |
| `JWT_SECRET` | Случайная строка (32+ символов) | ✅ |
| `JWT_EXPIRES_IN` | `7d` | ✅ |
| `FRONTEND_URL` | URL фронтенда | ✅ |

### Frontend (aimak-web)

| Переменная | Значение | Обязательная |
|------------|----------|--------------|
| `NODE_ENV` | `production` | ✅ |
| `NEXT_PUBLIC_API_URL` | URL API (с /api) | ✅ |
| `NEXT_PUBLIC_APP_NAME` | `Aimak Akshamy` | ❌ |
| `NEXT_PUBLIC_APP_DESCRIPTION` | `City Newspaper` | ❌ |

---

## ✅ После деплоя

### 1. Проверить здоровье сервисов

**Backend:**
```
https://aimak-api.onrender.com/api/health
```
Ответ должен быть:
```json
{
  "status": "ok",
  "timestamp": "2024-...",
  "uptime": 123.456,
  "environment": "production"
}
```

**Frontend:**
```
https://aimak-web.onrender.com
```
Должна открыться главная страница.

### 2. Проверить API документацию
```
https://aimak-api.onrender.com/api/docs
```

### 3. Создать первого администратора

1. Откройте фронтенд и зарегистрируйтесь
2. Подключитесь к БД через Render Dashboard:
   - PostgreSQL сервис → Info → External Database URL
   - Используйте любой PostgreSQL клиент или Prisma Studio
3. Найдите вашего пользователя в таблице `users`
4. Измените `role` на `ADMIN`
5. Обновите страницу в браузере

**Через psql:**
```bash
# Скопируйте External Database URL из Render
psql <External Database URL>

# В psql:
UPDATE users SET role = 'ADMIN' WHERE email = 'your@email.com';
```

### 4. Настроить Custom Domain (опционально)

1. В Render сервисе → Settings → Custom Domains
2. Добавьте ваш домен
3. Настройте DNS записи согласно инструкциям Render
4. Обновите переменные окружения с новым доменом

---

## 🐛 Troubleshooting

### Проблема: Build Failed

**Решение:**
1. Проверьте логи сборки в Render Dashboard
2. Убедитесь, что `pnpm` установлен (должен быть в package.json)
3. Проверьте, что все зависимости установлены

### Проблема: Сервис не запускается

**Решение:**
1. Проверьте переменные окружения
2. Убедитесь, что `DATABASE_URL` корректен
3. Проверьте, что Prisma миграции применились
4. Посмотрите логи сервиса

### Проблема: CORS ошибки

**Решение:**
1. Убедитесь, что `FRONTEND_URL` в API указывает на правильный URL фронтенда
2. Проверьте, что используется HTTPS
3. Переразверните API сервис

### Проблема: Database connection failed

**Решение:**
1. Проверьте, что PostgreSQL сервис запущен
2. Убедитесь, что используется **Internal Database URL** (не External)
3. Проверьте, что регион API и БД совпадают

### Проблема: Prisma migrations not working

**Решение:**
```bash
# Подключитесь к БД вручную
psql <External Database URL>

# Проверьте таблицы
\dt

# Если таблиц нет, примените миграции вручную через Prisma Studio
```

### Проблема: Free tier services sleep

Render Free tier сервисы "засыпают" после 15 минут неактивности и просыпаются при первом запросе (может занять 30-60 секунд).

**Решения:**
1. Обновитесь до платного плана ($7/месяц)
2. Используйте UptimeRobot для пинга каждые 5 минут
3. Используйте Render Cron Jobs для периодических запросов

---

## 💰 Стоимость

### Free Tier (Бесплатно)
- PostgreSQL: 256MB RAM, 1GB storage
- Redis: 25MB RAM
- Web Services: 512MB RAM, засыпают после 15 мин неактивности
- **Итого: $0/месяц**

### Рекомендуемый Starter Plan (~$21/месяц)
- PostgreSQL Starter: $7/месяц (256MB RAM, 1GB storage)
- Redis: Бесплатно (25MB)
- Backend Web Service: $7/месяц (512MB RAM, всегда активен)
- Frontend Web Service: $7/месяц (512MB RAM, всегда активен)

---

## 📚 Полезные ссылки

- [Render Docs](https://render.com/docs)
- [Render Blueprint Spec](https://render.com/docs/blueprint-spec)
- [Render Node.js Guide](https://render.com/docs/deploy-node-express-app)
- [Render PostgreSQL](https://render.com/docs/databases)
- [Render Environment Groups](https://render.com/docs/environment-variables)

---

## 🎉 Готово!

Ваш проект должен быть успешно задеплоен на Render.com!

**URLs:**
- Frontend: `https://aimak-web.onrender.com`
- Backend API: `https://aimak-api.onrender.com/api`
- API Docs: `https://aimak-api.onrender.com/api/docs`

Если возникнут проблемы, проверьте логи в Render Dashboard или обратитесь к разделу Troubleshooting выше.
