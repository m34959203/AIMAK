# Инструкция по деплою и инициализации БД

## 🚀 Деплой на Render.com

При каждом push в главную ветку Render автоматически:
1. ✅ Устанавливает зависимости
2. ✅ Генерирует Prisma клиент
3. ✅ Применяет миграции БД (`prisma migrate deploy`)
4. ✅ Собирает приложение

**ВАЖНО:** Инициализация БД (seed) НЕ запускается автоматически!

## 🗄️ Инициализация базы данных

### Метод 1: Через API Endpoint (рекомендуется для production)

После первого деплоя выполните один раз:

```bash
curl -X POST https://aimak-api-w8ps.onrender.com/api/setup/initialize
```

Или откройте в браузере и используйте Postman/Thunder Client:
```
POST https://aimak-api-w8ps.onrender.com/api/setup/initialize
```

**Что создается:**
- ✅ Админ пользователь:
  - Email: `admin@aimakakshamy.kz`
  - Password: `admin123`
  - Role: ADMIN

- ✅ 6 базовых категорий

**Безопасность:**
- Endpoint проверяет существование данных перед созданием
- Если админ или категории уже существуют, они не будут пересозданы
- Можно вызывать многократно - данные не дублируются

### Метод 2: Локальная инициализация

```bash
cd apps/api
pnpm db:seed
```

## 📊 Проверка состояния БД

```bash
# Проверить категории
curl https://aimak-api-w8ps.onrender.com/api/categories

# Проверить health
curl https://aimak-api-w8ps.onrender.com/api/health
```

## 🔑 Вход в админ-панель

1. URL: https://aimak-web-rvep.onrender.com/admin
2. Email: `admin@aimakakshamy.kz`
3. Password: `admin123`

**ВАЖНО:** Смените пароль после первого входа!

## 📝 Импорт статей

Статьи не создаются автоматически. Создайте их:
- Вручную через админ-панель (рекомендуется)
- Или импортируйте из WordPress: `pnpm import:wordpress:full`

## 🔄 Персистентность данных

✅ **База данных PostgreSQL на Render.com - персистентная**

- Данные сохраняются между деплоями
- БД не очищается при обновлении кода
- Миграции применяются, но данные остаются

## 🐛 Troubleshooting

### "Admin user already exists"
Это нормально. Админ уже создан.

### Статьи пропали
Статьи не создаются автоматически. Импортируйте заново или создайте вручную.

### 500 ошибка в админке
1. Проверьте инициализацию: `POST /api/setup/initialize`
2. Проверьте логи: Render Dashboard → aimak-api → Logs

## 🔐 Безопасность production

1. Смените пароль админа
2. Настройте S3 для файлов
3. Настройте SMTP для email
