# 🚀 Создание админа на Render.com

## Проблема
После деплоя не можете войти с учёткой админа.

## Причина
Seed скрипт не запустился автоматически или деплой был сделан до коммита с seed.

## ✅ Решение: Запустить скрипт вручную через Render Shell

### Шаг 1: Откройте Shell на Render

1. Откройте Render Dashboard
2. Перейдите в сервис `aimak-api`
3. В верхнем меню найдите **"Shell"** и нажмите на него
4. Откроется терминал с доступом к вашему приложению

### Шаг 2: Запустите скрипт создания админа

В открывшемся Shell выполните команды:

```bash
cd apps/api
pnpm db:create-admin
```

### Шаг 3: Проверьте результат

Вы должны увидеть:
```
🔧 Creating admin user...
✅ Connected to database
✅ Admin user created successfully!
   Email: admin@aimakakshamy.kz
   Password: admin123
   Role: ADMIN
   ID: [uuid]

✨ Done! You can now login with:
   Email: admin@aimakakshamy.kz
   Password: admin123
```

### Шаг 4: Войдите в систему

Теперь можете войти на сайте:
- URL: https://aimak-web-rvep.onrender.com/ru/login
- Email: `admin@aimakakshamy.kz`
- Password: `admin123`

---

## 🔄 Альтернатива: Автоматический seed при деплое

Если хотите, чтобы админ создавался автоматически:

1. Убедитесь, что в `render.yaml` в buildCommand есть `pnpm db:seed`:
   ```yaml
   buildCommand: pnpm install --frozen-lockfile && cd apps/api && pnpm prisma generate && pnpm migrate:deploy && pnpm db:seed && pnpm build
   ```

2. Сделайте Manual Deploy в Render Dashboard

---

## 🔍 Проверка состояния БД

Если нужно проверить, есть ли админ в базе:

```bash
cd apps/api
pnpm prisma studio
```

Откроется Prisma Studio, где можно посмотреть всех пользователей.

---

## ⚠️ Важно

- Скрипт **идемпотентный** - можно запускать несколько раз
- Если админ уже существует, скрипт обновит его пароль на `admin123`
- Данные админа одинаковые на всех окружениях

---

## 🆘 Troubleshooting

### Ошибка "Cannot find module '@prisma/client'"
```bash
cd apps/api
pnpm prisma generate
pnpm db:create-admin
```

### Ошибка подключения к БД
Проверьте, что переменная `DATABASE_URL` установлена в Environment Variables.

### Скрипт завершается с ошибкой
Посмотрите полный текст ошибки и проверьте:
- Есть ли подключение к БД
- Запущены ли миграции (`pnpm migrate:deploy`)
