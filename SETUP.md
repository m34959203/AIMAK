# 🚀 Руководство по настройке и запуску проекта AIMAK

## 📋 Содержание

1. [Требования](#требования)
2. [Установка](#установка)
3. [Настройка базы данных](#настройка-базы-данных)
4. [Запуск приложения](#запуск-приложения)
5. [Создание тестовых данных](#создание-тестовых-данных)
6. [Доступ к приложению](#доступ-к-приложению)
7. [Решение проблем](#решение-проблем)

---

## Требования

- **Node.js** 20+ (LTS)
- **pnpm** 8+
- **Docker** и **Docker Compose**
- **PostgreSQL** 15+ (через Docker)
- **Git**

---

## Установка

### 1. Клонирование репозитория

```bash
git clone https://github.com/m34959203/AIMAK.git
cd AIMAK
```

### 2. Установка зависимостей

```bash
# Установка pnpm (если не установлен)
npm install -g pnpm

# Установка зависимостей всех приложений
pnpm install
```

---

## Настройка базы данных

### 1. Запуск PostgreSQL и Redis через Docker

```bash
docker-compose up -d postgres redis
```

### 2. Настройка переменных окружения

Создайте файлы `.env` в директориях:

#### `apps/api/.env`

```env
# Database
DATABASE_URL="postgresql://postgres:password@localhost:5432/aimak?schema=public"

# Redis
REDIS_URL="redis://localhost:6379"

# JWT
JWT_SECRET="your-super-secret-jwt-key-change-this-in-production"
JWT_EXPIRES_IN="15m"
JWT_REFRESH_SECRET="your-super-secret-refresh-key-change-this"
JWT_REFRESH_EXPIRES_IN="7d"

# API
PORT=4000
API_PREFIX=api

# CORS
CORS_ORIGIN="http://localhost:3000"

# AI Providers (опционально для AI функций)
GEMINI_API_KEY="your-gemini-api-key"
OPENAI_API_KEY="your-openai-api-key"
```

#### `apps/web/.env.local`

```env
NEXT_PUBLIC_API_URL=http://localhost:4000
```

### 3. Применение миграций Prisma

```bash
cd apps/api

# Генерация Prisma Client
npx prisma generate

# Применение миграций к БД
npx prisma migrate dev --name initial_bilingual_schema

# Вернуться в корень
cd ../..
```

### 4. Создание категорий из ТД

```bash
cd apps/api

# Запуск seed скрипта
npx ts-node prisma/seeds/categories.seed.ts

cd ../..
```

Будут созданы 6 категорий:
- ✅ ЖАҢАЛЫҚТАР / НОВОСТИ (zhanalyqtar)
- ✅ ӨЗЕКТІ / АКТУАЛЬНО (ozekti)
- ✅ САЯСАТ / ПОЛИТИКА (sayasat)
- ✅ МӘДЕНИЕТ / КУЛЬТУРА (madeniyet)
- ✅ ҚОҒАМ / ОБЩЕСТВО (qogam)
- ✅ KAZAKHMYS NEWS (kazakhmys)

---

## Запуск приложения

### Вариант 1: Раздельный запуск (для разработки)

Откройте 2 терминала:

**Терминал 1 - Backend:**
```bash
cd apps/api
pnpm dev
```

**Терминал 2 - Frontend:**
```bash
cd apps/web
pnpm dev
```

### Вариант 2: Docker Compose (для продакшена)

```bash
docker-compose up -d
```

Это запустит:
- PostgreSQL (порт 5432)
- Redis (порт 6379)
- API (порт 4000)
- Web (порт 3000)

---

## Создание тестовых данных

### 1. Создание первого администратора

```bash
# Зарегистрируйте пользователя через UI
# Затем повысьте его до админа:

curl -X POST http://localhost:4000/api/users/create-first-admin \
  -H "Content-Type: application/json" \
  -d '{"email": "your-email@example.com"}'
```

### 2. Создание тестовых статей (через Prisma Studio)

```bash
cd apps/api
npx prisma studio
```

Откроется http://localhost:5555 - графический интерфейс для управления БД.

Создайте статью со следующими полями:
- **titleKz**: "Қарағандының соңғы жаңалықтары"
- **titleRu**: "Последние новости Караганды"
- **contentKz**: "Мақала мазмұны казахша..."
- **contentRu**: "Содержание статьи на русском..."
- **slugKz**: "qaragandy-zhanalyqtary"
- **slugRu**: "novosti-karagandy"
- **categoryId**: [выберите из Categories]
- **authorId**: [ваш user ID]
- **status**: PUBLISHED
- **published**: true
- **publishedAt**: [текущая дата]

---

## Доступ к приложению

| Сервис | URL | Описание |
|--------|-----|----------|
| **Frontend (Казахский)** | http://localhost:3000/kz | Главная страница (KZ) |
| **Frontend (Русский)** | http://localhost:3000/ru | Главная страница (RU) |
| **Admin Panel** | http://localhost:3000/admin | Панель редактора |
| **API** | http://localhost:4000 | REST API |
| **API Docs (Swagger)** | http://localhost:4000/api/docs | Документация API |
| **Prisma Studio** | http://localhost:5555 | БД интерфейс |

### Тестовые учетные данные

После регистрации и повышения до админа:
- Email: ваш email
- Password: ваш пароль
- Role: ADMIN

---

## Структура маршрутов

```
/                           → Redirect на /kz
/kz                         → Главная (казахский)
/ru                         → Главная (русский)

/[lang]/zhanalyqtar         → Новости
/[lang]/ozekti              → Актуально
/[lang]/sayasat             → Политика
/[lang]/madeniyet           → Культура
/[lang]/qogam               → Общество
/[lang]/kazakhmys           → Kazakhmys News

/[lang]/[category]/[slug]   → Страница статьи

/admin                      → Админ панель
/admin/articles             → Управление статьями
/admin/articles/new         → Создать статью
/admin/articles/[id]        → Редактировать статью
/admin/categories           → Управление категориями
/admin/tags                 → Управление тегами
```

---

## Решение проблем

### Проблема: Ошибка подключения к БД

**Решение:**
```bash
# Проверьте, запущен ли PostgreSQL
docker ps | grep postgres

# Перезапустите контейнер
docker-compose restart postgres

# Проверьте логи
docker-compose logs postgres
```

### Проблема: Prisma Client не сгенерирован

**Решение:**
```bash
cd apps/api
npx prisma generate
```

### Проблема: Миграции не применились

**Решение:**
```bash
cd apps/api

# Сброс БД (ВНИМАНИЕ: удалит все данные!)
npx prisma migrate reset

# Или применить миграции заново
npx prisma migrate dev
```

### Проблема: Порт уже занят

**Решение:**
```bash
# Проверить, что использует порт
lsof -i :4000  # для API
lsof -i :3000  # для Web

# Убить процесс
kill -9 <PID>
```

### Проблема: CORS ошибки

**Решение:**
Убедитесь, что в `apps/api/.env`:
```env
CORS_ORIGIN="http://localhost:3000"
```

### Проблема: Статьи не отображаются

**Проверьте:**
1. Поле `published` = `true`
2. Поле `status` = `PUBLISHED`
3. Поле `publishedAt` установлено
4. Поля `titleKz` и `contentKz` заполнены

---

## Полезные команды

### Prisma

```bash
# Открыть Prisma Studio
cd apps/api && npx prisma studio

# Создать новую миграцию
npx prisma migrate dev --name your_migration_name

# Применить миграции
npx prisma migrate deploy

# Сбросить БД (удаляет все данные!)
npx prisma migrate reset

# Форматировать schema.prisma
npx prisma format
```

### Docker

```bash
# Запустить все сервисы
docker-compose up -d

# Остановить все сервисы
docker-compose down

# Просмотреть логи
docker-compose logs -f

# Просмотреть логи конкретного сервиса
docker-compose logs -f api

# Пересобрать контейнеры
docker-compose up -d --build

# Удалить все контейнеры и volumes
docker-compose down -v
```

### pnpm

```bash
# Установить зависимости
pnpm install

# Запустить dev сервер
pnpm dev

# Собрать для продакшена
pnpm build

# Запустить продакшен сервер
pnpm start

# Линтинг
pnpm lint

# Проверка типов
pnpm type-check
```

---

## Следующие шаги

После успешного запуска:

1. ✅ Создайте администратора
2. ✅ Создайте несколько тестовых статей
3. ✅ Добавьте теги к статьям
4. ✅ Протестируйте переключение языков
5. ✅ Проверьте работу админ-панели

### Дальнейшая разработка

См. [TENGRI_MIGRATION.md](./TENGRI_MIGRATION.md) для:
- Обновления админ-панели для билингвального контента
- Интеграции AI генерации
- Добавления системы комментариев
- Настройки рекламных блоков

---

## 📚 Документация

- [Техническая документация (ТД)](./TD.md)
- [Миграция на Tengrinews](./TENGRI_MIGRATION.md)
- [Prisma Docs](https://www.prisma.io/docs)
- [Next.js Docs](https://nextjs.org/docs)
- [NestJS Docs](https://docs.nestjs.com)

---

**Успешного запуска! 🚀**

*Версия: 1.0.0*
*Дата: 2025-11-17*
